# Environment example for local development.
# Keep real secrets only in `.env`; never commit them.

# No standard KEY=value entries were detected in the current local `.env`.
# Add required keys here with empty or safe placeholder values as the app config is normalized.
